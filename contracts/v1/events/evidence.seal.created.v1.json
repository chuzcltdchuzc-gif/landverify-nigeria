{
  "$id": "https://aquasavannah.landvault/contracts/v1/events/evidence.seal.created.v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Domain event emitted by the `evidence` bounded context for aggregate `Seal`. Schema validates the full envelope; `payload` carries the aggregate-specific fields documented under `x-payload-fields`.",
  "properties": {
    "actor": {
      "type": [
        "string",
        "null"
      ]
    },
    "aggregate_id": {
      "type": "string"
    },
    "aggregate_type": {
      "const": "Seal",
      "type": "string"
    },
    "aggregate_version": {
      "minimum": 0,
      "type": "integer"
    },
    "causation_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "correlation_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "country": {
      "type": [
        "string",
        "null"
      ]
    },
    "event_id": {
      "pattern": "^evt_[a-f0-9]{32}$",
      "type": "string"
    },
    "event_type": {
      "const": "evidence.seal.created",
      "type": "string"
    },
    "event_version": {
      "const": 1,
      "type": "integer"
    },
    "occurred_at": {
      "format": "date-time",
      "type": "string"
    },
    "organization_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "payload": {
      "additionalProperties": true,
      "properties": {
        "created_by": {
          "description": "string — principal id",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "evidence_ids": {
          "description": "string[] — items included in the seal manifest",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "item_count": {
          "description": "integer",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "manifest_hash": {
          "description": "string — SHA-256 hex of canonical_json(manifest)",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "merkle_root": {
          "description": "string — SHA-256 hex digest of the canonical merkle tree",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "registry_id": {
          "description": "string",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "seal_id": {
          "description": "string — immutable seal id",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "producer": {
      "const": "evidence",
      "type": "string"
    },
    "tenant_id": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "event_id",
    "event_type",
    "event_version",
    "aggregate_type",
    "aggregate_id",
    "aggregate_version",
    "occurred_at",
    "producer",
    "payload"
  ],
  "title": "evidence.seal.created",
  "type": "object",
  "x-aggregate": "Seal",
  "x-bounded-context": "evidence",
  "x-contract-version": "2.0.0",
  "x-event-name": "evidence.seal.created",
  "x-event-version": 1,
  "x-idempotency": "Dedup by `event_id`.",
  "x-known-consumers": [
    "audit-log",
    "metrics",
    "verification",
    "certificate"
  ],
  "x-ordering": "Per-Seal ordering by `aggregate_version`.",
  "x-payload-fields": {
    "created_by": "string — principal id",
    "evidence_ids": "string[] — items included in the seal manifest",
    "item_count": "integer",
    "manifest_hash": "string — SHA-256 hex of canonical_json(manifest)",
    "merkle_root": "string — SHA-256 hex digest of the canonical merkle tree",
    "registry_id": "string",
    "seal_id": "string — immutable seal id"
  },
  "x-producer": "evidence",
  "x-replay": "Idempotent."
}
