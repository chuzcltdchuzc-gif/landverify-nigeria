{
  "$id": "https://aquasavannah.landvault/contracts/v1/events/evidence.lock.extended.v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Domain event emitted by the `evidence` bounded context for aggregate `EvidenceLock`. Schema validates the full envelope; `payload` carries the aggregate-specific fields documented under `x-payload-fields`.",
  "properties": {
    "actor": {
      "type": [
        "string",
        "null"
      ]
    },
    "aggregate_id": {
      "type": "string"
    },
    "aggregate_type": {
      "const": "EvidenceLock",
      "type": "string"
    },
    "aggregate_version": {
      "minimum": 0,
      "type": "integer"
    },
    "causation_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "correlation_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "country": {
      "type": [
        "string",
        "null"
      ]
    },
    "event_id": {
      "pattern": "^evt_[a-f0-9]{32}$",
      "type": "string"
    },
    "event_type": {
      "const": "evidence.lock.extended",
      "type": "string"
    },
    "event_version": {
      "const": 1,
      "type": "integer"
    },
    "occurred_at": {
      "format": "date-time",
      "type": "string"
    },
    "organization_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "payload": {
      "additionalProperties": true,
      "properties": {
        "by": {
          "description": "string",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "evidence_id": {
          "description": "string",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "lock_id": {
          "description": "string",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "new_until": {
          "description": "string ISO8601",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "previous_until": {
          "description": "string ISO8601",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "reason": {
          "description": "string",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "producer": {
      "const": "evidence",
      "type": "string"
    },
    "tenant_id": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "event_id",
    "event_type",
    "event_version",
    "aggregate_type",
    "aggregate_id",
    "aggregate_version",
    "occurred_at",
    "producer",
    "payload"
  ],
  "title": "evidence.lock.extended",
  "type": "object",
  "x-aggregate": "EvidenceLock",
  "x-bounded-context": "evidence",
  "x-contract-version": "1.5.0",
  "x-event-name": "evidence.lock.extended",
  "x-event-version": 1,
  "x-idempotency": "Dedup by `event_id`.",
  "x-known-consumers": [
    "audit-log",
    "metrics"
  ],
  "x-ordering": "Per-EvidenceLock ordering by `aggregate_version`.",
  "x-payload-fields": {
    "by": "string",
    "evidence_id": "string",
    "lock_id": "string",
    "new_until": "string ISO8601",
    "previous_until": "string ISO8601",
    "reason": "string"
  },
  "x-producer": "evidence",
  "x-replay": "Idempotent; forward-only retention."
}
