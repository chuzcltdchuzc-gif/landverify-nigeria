{
  "$id": "https://aquasavannah.landvault/contracts/v1/events/identity.user.registered.v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Domain event emitted by the `identity` bounded context for aggregate `User`. Schema validates the full envelope; `payload` carries the aggregate-specific fields documented under `x-payload-fields`.",
  "properties": {
    "actor": {
      "type": [
        "string",
        "null"
      ]
    },
    "aggregate_id": {
      "type": "string"
    },
    "aggregate_type": {
      "const": "User",
      "type": "string"
    },
    "aggregate_version": {
      "minimum": 0,
      "type": "integer"
    },
    "causation_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "correlation_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "country": {
      "type": [
        "string",
        "null"
      ]
    },
    "event_id": {
      "pattern": "^evt_[a-f0-9]{32}$",
      "type": "string"
    },
    "event_type": {
      "const": "identity.user.registered",
      "type": "string"
    },
    "event_version": {
      "const": 1,
      "type": "integer"
    },
    "occurred_at": {
      "format": "date-time",
      "type": "string"
    },
    "organization_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "payload": {
      "additionalProperties": true,
      "properties": {
        "country": {
          "description": "string|null — ISO-3166-1 alpha-2",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "email": {
          "description": "string — normalized lowercase email",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "registration_source": {
          "description": "string — 'local' | 'google'",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "roles": {
          "description": "string[] — initial role set",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "user_id": {
          "description": "string — identity user id (uuid)",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "producer": {
      "const": "identity",
      "type": "string"
    },
    "tenant_id": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "event_id",
    "event_type",
    "event_version",
    "aggregate_type",
    "aggregate_id",
    "aggregate_version",
    "occurred_at",
    "producer",
    "payload"
  ],
  "title": "identity.user.registered",
  "type": "object",
  "x-aggregate": "User",
  "x-bounded-context": "identity",
  "x-contract-version": "2.0.0",
  "x-event-name": "identity.user.registered",
  "x-event-version": 1,
  "x-idempotency": "Consumers MUST deduplicate by `event_id`. Producers guarantee exactly-once via the transactional outbox.",
  "x-known-consumers": [
    "audit-log",
    "metrics",
    "notifications"
  ],
  "x-ordering": "Per-aggregate (User) ordering by `aggregate_version`. No global ordering across aggregates.",
  "x-payload-fields": {
    "country": "string|null — ISO-3166-1 alpha-2",
    "email": "string — normalized lowercase email",
    "registration_source": "string — 'local' | 'google'",
    "roles": "string[] — initial role set",
    "user_id": "string — identity user id (uuid)"
  },
  "x-producer": "identity",
  "x-replay": "Idempotent — events can be replayed from the outbox."
}
