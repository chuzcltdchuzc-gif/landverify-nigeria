{
  "$id": "https://aquasavannah.landvault/contracts/v1/events/evidence.anchor.replayed.v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Domain event emitted by the `evidence` bounded context for aggregate `AnchorBatch`. Schema validates the full envelope; `payload` carries the aggregate-specific fields documented under `x-payload-fields`.",
  "properties": {
    "actor": {
      "type": [
        "string",
        "null"
      ]
    },
    "aggregate_id": {
      "type": "string"
    },
    "aggregate_type": {
      "const": "AnchorBatch",
      "type": "string"
    },
    "aggregate_version": {
      "minimum": 0,
      "type": "integer"
    },
    "causation_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "correlation_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "country": {
      "type": [
        "string",
        "null"
      ]
    },
    "event_id": {
      "pattern": "^evt_[a-f0-9]{32}$",
      "type": "string"
    },
    "event_type": {
      "const": "evidence.anchor.replayed",
      "type": "string"
    },
    "event_version": {
      "const": 1,
      "type": "integer"
    },
    "occurred_at": {
      "format": "date-time",
      "type": "string"
    },
    "organization_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "payload": {
      "additionalProperties": true,
      "properties": {
        "batch_id": {
          "description": "string — DLQ batch id",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "provider_id": {
          "description": "string",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        },
        "replayed_to": {
          "description": "string — new batch id",
          "type": [
            "string",
            "number",
            "boolean",
            "object",
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "producer": {
      "const": "evidence",
      "type": "string"
    },
    "tenant_id": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "event_id",
    "event_type",
    "event_version",
    "aggregate_type",
    "aggregate_id",
    "aggregate_version",
    "occurred_at",
    "producer",
    "payload"
  ],
  "title": "evidence.anchor.replayed",
  "type": "object",
  "x-aggregate": "AnchorBatch",
  "x-bounded-context": "evidence",
  "x-contract-version": "1.5.0",
  "x-event-name": "evidence.anchor.replayed",
  "x-event-version": 1,
  "x-idempotency": "Dedup by `event_id`. Original DLQ batch is frozen.",
  "x-known-consumers": [
    "audit-log",
    "verification"
  ],
  "x-ordering": "Per-AnchorBatch ordering. References a NEW batch_id.",
  "x-payload-fields": {
    "batch_id": "string — DLQ batch id",
    "provider_id": "string",
    "replayed_to": "string — new batch id"
  },
  "x-producer": "evidence",
  "x-replay": "Idempotent. New batch goes through the full saga again."
}
